---
status: skeleton
---

# API Contract

> **Owns:** Every endpoint: method, path, request/response payloads, plus the API-wide conventions (errors, pagination, filtering).

## Conventions
_(empty — URL style, auth header, versioning, date/number formats)_

## Standard error response
_(empty — one error shape used by every endpoint: structure, error codes, validation-error format)_

## Pagination, filtering & sorting
_(empty — the one standard mechanism, used everywhere lists are returned)_

## Endpoints
_(empty — grouped by resource; one `API-` ID per endpoint: method, path, permission required (PERM refs), request body, response body, rules enforced (BR refs), error cases)_

## Open decisions
- [ ] REST, RPC-style, or something else?
- [ ] What does the standard error payload look like?
- [ ] Offset or cursor pagination?
- [ ] Is the API versioned from day one?
